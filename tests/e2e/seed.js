(()=>{const c={};for(const w of WEEKS)for(const ch of w.channels){const k=FP.channelKey(ch);c[k]={t:Date.now(),vids:Array.from({length:6},(_,i)=>({id:k+'_v'+i,title:ch.name+' — vídeo de teste número '+(i+1)+' com um título um pouco longo pra testar quebra de linha',thumb:'data:image/svg+xml,'+encodeURIComponent('<svg xmlns="http://www.w3.org/2000/svg" width="320" height="180"><rect width="320" height="180" fill="#252D3D"/><path d="M130 65v50l45-25z" fill="#F59E0B"/></svg>'),published:new Date(Date.now()-i*172800000).toISOString(),channel:ch.name}))}}
localStorage.setItem('fp-config',JSON.stringify({apiKey:'TEST',gistToken:'TESTTOKEN'}));localStorage.setItem('fp-vidcache',JSON.stringify(c));localStorage.removeItem('fp-progress');return 'seeded '+Object.keys(c).length})()
